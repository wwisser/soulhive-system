package de.soulhive.system.container;

public enum ContainerStorageLevel {

    /**
     * Generates a container once and stores it as field.
     */
    STORED,
    /**
     * Generates a container every time again.
     */
    NEW

}
